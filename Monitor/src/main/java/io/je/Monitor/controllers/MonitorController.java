package io.je.Monitor.controllers;

import org.springframework.stereotype.Controller;

@Controller
public class MonitorController {

    /*@Autowired
    WebSocketService service;

    @Autowired
    SimpMessageSendingOperations messagingTemplate;

    @MessageMapping("/testPayload")
    //@SendTo("/rule/ruleUpdates")
    public  void testPayload(String payload) throws Exception {
        Thread.sleep(1000);
        messagingTemplate.convertAndSend("/rule/ruleUpdates", payload);
        service.sendUpdates(payload);// simulated delay
        //return "testtt";

    }*/
}
